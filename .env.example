NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Set to true to skip Upstash entirely (local dev). If KV_* is set but the host is dead/wrong, the app auto-falls back to memory after the first failed request.
# KV_USE_MEMORY=true
GOOGLE_SHEETS_SPREADSHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
